[project]
name = "10s-comfy-nodes"
version = "1.9.6"
description = "Identity preservation, latent anchoring, upscale-pass quality, and STG guidance nodes for the LTX2 video diffusion model"
license = { text = "MIT" }
requires-python = ">=3.10"

[project.urls]
Repository = "https://github.com/TenStrip/10S-Comfy-nodes"

[tool.comfy]
PublisherId = "tenstrip"
DisplayName = "10S Nodes"
Icon = ""
